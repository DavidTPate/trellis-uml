name: Authorization Controller
description: Ensures that users are authorized to access the request resource for the requested action, rejects if not allowed
technology: Controller
relationships:
  - type: uses
    target:
      $ref: './Security-Component.yaml'
